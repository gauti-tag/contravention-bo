version: '3.4'

services:
  web:
    image: ngser/contravention-guinee-admin:latest
    container_name: contravention-guinee-admin
    build: .
    depends_on:     
      - database
      - redis
    ports:
      - "5091:3000"
    volumes:
      - .:/home/ngser/app
    env_file:
      - .env
    environment:
      - DB_USERNAME=$POSTGRES_USER
      - DB_PASSWORD=$POSTGRES_PASSWORD
  database:
    image: postgres:13.2-alpine
    restart: always
    ports: 
      - "5482:5432"
    volumes:
      - "${DB_DATA_CONTRAVENTIONADMIN}:/var/lib/postgresql/data"
    environment:
      # Setting postgresql credentials
      - POSTGRES_USER
      - POSTGRES_PASSWORD
      - POSTGRES_DB=contravention_admin_dev

  redis:
    image: redis:6.2.1-alpine
    restart: always
