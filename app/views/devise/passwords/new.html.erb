<main>
    <!-- Section -->
    <section class="vh-lg-100 mt-5 mt-lg-0 bg-soft d-flex align-items-center">
        <div class="container">
            <div class="row justify-content-center form-bg-image">
                <p class="text-center">
                  <%= link_to new_user_session_path, class: 'd-flex align-items-center justify-content-center' do %>
                    <svg class="icon icon-xs me-2" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" d="M7.707 14.707a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414l4-4a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l2.293 2.293a1 1 0 010 1.414z" clip-rule="evenodd"></path></svg>
                    Se connecter
                  <% end %>
                </p>
                <div class="col-12 d-flex align-items-center justify-content-center">
                    <div class="signin-inner my-3 my-lg-0 bg-white shadow border-0 rounded p-4 p-lg-5 w-100 fmxw-500">
                        <h1 class="h3">Mot de passe oublié ?</h1>
                        <p class="mb-4">Renseignez votre adresse e-mail. Un mail vous sera envoyé avec des instructions.</p>
                        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
                            <!-- Form -->
                            <div class="mb-4">
                                <%= f.label :email, 'Adresse email' %>
                                <div class="input-group">
                                    <%= f.email_field :email, autofocus: true, required: true, placeholder: "monadresse@email.com", class: 'form-control' %>
                                </div>  
                            </div>
                            <!-- End of Form -->
                            <div class="d-grid">
                                <%= f.submit "Valider", class: 'btn btn-gray-800' %>
                            </div>
                        <% end %>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>