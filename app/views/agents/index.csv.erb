<%= fields = ['NOM', 'PRENOMS', 'GRADE', 'NUMERO_DU_BADGE'].join(";") %>
<% @agents.each do |agent| %>
<% nom = agent.last_name %>
<% prenoms = agent.first_name %>
<% grade = agent.grade %>
<% numero_grade = agent.identifier %>
<%= [nom, prenoms, grade, numero_grade].join(";")  %>
<% end %>